#!/usr/bin/env bash

BPYTOP="$HOME/.local/bin/bpytop"

if [ ! -x "$BPYTOP" ]; then
    python3 -m pip install --user --disable-pip-version-check -q bpytop >/dev/null 2>&1
fi

if [ ! -x "$BPYTOP" ]; then
    echo "Failed to start AFK mode: bpytop is unavailable."
    exit 1
fi

exec "$BPYTOP"